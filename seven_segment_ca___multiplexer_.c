// Common Anode (CA) ????? ??? (0-9)
unsigned char array[] = {0xC0, 0xF9, 0xA4, 0xb0, 0x99, 0x92, 0x82, 0xF8, 0x80, 0x90};
unsigned int i, j, count = 0;

void main() {
    TRISB = 0x00;   // PORTB ?????? (Data Pins)
    TRISA = 0x00;   // PORTA ?????? (Control Pins)
    PORTB = 0xFF;   // ?????? ?? ????
    PORTA = 0x00;   // CA ??????? ?? ???? ??? High (1) ???? ???

    while(1) {
        // ? ???? ?? ??????? ?????? ???? ????
        for(count = 0; count < 100; count++) {
            // ??????? ???????? ???????? ????????? ??????? ???? ???
            for(j = 0; j < 50; j++) {

                // ????? ????? (???) - RA0 (??? ?)
                PORTB = array[count / 10]; // ????? ????
                PORTA = 0x01;              // RA0 ??? (??????? ? ??)
                delay_ms(10);
                PORTA = 0x00;              // ??????? ?? (??????? ?????)

                // ??????? ????? (???) - RA1 (??? ?)
                PORTB = array[count % 10]; // ????? ????
                PORTA = 0x02;              // RA1 ??? (??????? ? ??)
                delay_ms(10);
                PORTA = 0x00;
            }
        }
    }
}