// Common Anode (CA) values for 0-9
unsigned char array[]={0xc0, 0xf9, 0xa4, 0xb0, 0x99, 0x92, 0x82, 0xf8, 0x80, 0x90};
int i;

void main() {
    TRISB = 0x00;   // PORTB-?? ?????? ?????? ??? ???
    PORTB = 0xFF;   // CA-?? ?????? ?? ???? ????? FF (?? High) ??????? ??? ??

    while(1) {
        // ? ???? ? ??????? ??????
        for(i=0; i<10; i++) {
            PORTB = array[i];
            delay_ms(1000);
        }

        //??? ? ??????? ?????? ? ?
        for(i=9; i>=0; i--) {
            PORTB = array[i];
            delay_ms(1000);
        }
    }
}